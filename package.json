{
  "name": "punch-in-app-ai",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npm run build && node backend/index.js"
  },
  "type": "module",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "couchbase": "^4.2.5"
  }
}
